# Legacy reference retained without deleting history.
#
# GitHub does not execute workflows from this directory. The active,
# security-pinned community validation workflow is:
#
#   ../.github/workflows/blank.yml
#
# New automation must be added under ../.github/workflows/ and reviewed through
# the normal pull-request process.

status: legacy
active: false
canonical_workflow: ../.github/workflows/blank.yml
